//reducer返回新的状态

// 菜单组件
const initialState = {
     key: 0
}

export default function (state = initialState, action) {
     switch (action.type) {
          default:
               return state
     }
}